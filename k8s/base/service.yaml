apiVersion: v1
kind: Service
metadata:
  name: ticketing-service
  namespace: ticketing
spec:
  selector:
    app: ticketing-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
