serverFiles:
  prometheus.yml:
    scrape_configs:
      - job_name: ticketing-service
        metrics_path: /metrics
        static_configs:
          - targets:
              - ticketing-service.ticketing.svc.cluster.local:80
